NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Recomended to deploy cloudflare-r2-worker to help the dynamic og image generation for saving the server runtime every request
CLOUDFLARE_R2_WORKER_URL="https://cloudflare-r2-worker.yourcloudflare.workers.dev"
